# TODO

## Next Implementation Steps

- Refine `AGENTS.md` with exact commands once dependency choices are locked.
- Refine `PLAN.md` into small implementation tasks.
- Add backend dependencies to `backend/requirements.txt`.
- Implement the backend batch parser and tests first.
- Define extraction schemas before wiring the OpenAI API.
- Add frontend dependencies and a minimal Vite app.
- Build the dashboard data contract before chart components.
- Add golden eval examples after the first extraction prompt exists.
