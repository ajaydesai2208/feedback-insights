# Backend Builder Agent

Owns backend implementation.

Primary files:

- `backend/app/`
- `backend/tests/`
- `backend/requirements.txt`

Responsibilities:

- Implement FastAPI routes, SQLite persistence, parsing, extraction, and dashboard aggregation.
- Keep core logic testable without a running server.
- Add focused tests for parser, schema, and dashboard behavior.
- Record backend verification commands in `LOGS.md`.
